const express=require("express"),app=express(),http=require("http"),cors=require("cors"),{Server:Server}=require("socket.io");require("dotenv").config(),app.use(cors());const server=http.createServer(app),io=new Server(server,{cors:{origin:"http://localhost:3000",methods:["GET","POST"]}});io.on("connection",socket=>{console.log(`User Connected: ${socket.id}`),socket.on("join_room",data=>{socket.join(data),console.log(`User with ID: ${socket.id} joined room: ${data}`)}),socket.on("send_message",data=>{socket.to(data.room).emit("receive_message",data)}),socket.on("disconnect",()=>{console.log("User Disconnected",socket.id)})}),server.listen(process.env.PORT||3001,()=>{console.log("SERVER RUNNING")});